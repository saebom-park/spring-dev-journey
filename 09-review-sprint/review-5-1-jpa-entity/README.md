# [REVIEW-5-1] JPA 엔티티 설계 (jpa-entity)

> 💬 이번 실습에서는 JPA를 이용해 기본적인 엔티티 설계를 직접 구현해봅니다.
> 
> 
> 단일 엔티티를 정의하고, DB에 데이터를 저장하고 불러오는 가장 기초적인 흐름을
> 
> **Spring Boot + JPA + 3계층 구조**로 복습합니다.
> 
> 이전 단계까지 학습한 컨트롤러, 서비스, DTO, 레포지토리 구성을 **JPA 기반으로 다시 익히는 복습**이에요.
> 

---

## 💡 시나리오

쇼핑몰 관리 시스템에서 회원과 상품 데이터를 관리할 수 있는 기능을 추가하려고 합니다.

관리자는 상품과 회원을 등록하고, 전체 목록을 확인할 수 있어야 합니다.

---

## 📋 요구사항

- Spring Boot + JPA 프로젝트 기반으로 구성할 것
- `Member`, `Product` 도메인 클래스에 `@Entity` 어노테이션 적용
- `@Id`, `@GeneratedValue`, `@Column` 등 JPA 기본 매핑 사용
- 단순 등록/조회용 API 구현 (POST + GET)
- 요청/응답을 위한 DTO 클래스 구성
- 3계층 구조로 컨트롤러, 서비스, 레포지토리 설계

---

## 🎯 체크리스트

- ✅ Spring Boot + JPA 환경 구성
- ✅ `Member`, `Product` 엔티티 설계 (@Entity, @Id 등)
- ✅ 요청/응답 DTO 클래스 설계
- ✅ Repository 인터페이스 구성 (JpaRepository 상속)
- ✅ Service 클래스 구현
- ✅ Controller 클래스 구현
- ✅ POST /members, /products 등록 API 테스트
- ✅ GET /members, /products 조회 API 테스트

---

## 📂 폴더 구조

```
review-5-1/
├── src/
│   └── main/
│       ├── java/
│       │   └── com/
│       │       └── review51/
│       │           ├── controller/
│       │           │   ├── MemberController.java
│       │           │   └── ProductController.java
│       │           ├── service/
│       │           │   ├── MemberService.java
│       │           │   ├── MemberServiceImpl.java
│       │           │   ├── ProductService.java
│       │           │   └── ProductServiceImpl.java
│       │           ├── repository/
│       │           │   ├── MemberRepository.java
│       │           │   └── ProductRepository.java
│       │           ├── dto/
│       │           │   ├── MemberRequestDto.java
│       │           │   ├── MemberResponseDto.java
│       │           │   ├── ProductRequestDto.java
│       │           │   └── ProductResponseDto.java
│       │           ├── domain/
│       │           │   ├── Member.java
│       │           │   └── Product.java
│       │           └── JpaEntityApplication.java
│       └── resources/
│           └── application.yml
├── build.gradle
├── settings.gradle
├── README.md

```

---

> 이번 실습을 통해 JPA 엔티티 매핑의 기본 구조와 흐름을 다시 복습할 수 있습니다.
> 
> 
> 단방향 단일 테이블 구조로 시작해, 다음 실습에서는 연관관계 매핑으로 확장할 예정이에요! 🌱
>