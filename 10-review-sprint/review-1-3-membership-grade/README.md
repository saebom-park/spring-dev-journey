# REVIEW-1-3: 멤버십 등급 시스템 (membership-grade)

> 🎯 **목표:** 상속, 추상 클래스, 인터페이스, 다형성 개념을 복습하고  
> 등급별 멤버 혜택 시스템을 설계하는 실습입니다.

---

## 💡 실습 시나리오

💎 **멤버십 등급 시스템 (콘솔 기반)**

- `Member`는 공통 정보를 가진 추상 클래스  
- 실버/골드/플래티넘 등 **다양한 등급의 멤버 클래스**를 만들고  
- 각각 다른 할인율/포인트 적립률을 적용하며  
- 등급별 혜택 정보를 출력하는 시스템을 만듭니다.

---

## 📋 구현 요구사항

### 1. `Member` 추상 클래스
- `name`, `email`, `membershipGrade` 필드
- 추상 메서드: `getDiscountRate()`, `getPointRate()`
- 공통 출력 메서드 `toString()` 작성

### 2. `SilverMember` / `GoldMember` / `PlatinumMember` 클래스
- `Member`를 상속
- 각 클래스에서 할인율과 포인트 적립률을 다르게 구현

### 3. `BenefitPrintable` 인터페이스
- `printBenefitInfo()` 정의
- 모든 멤버 클래스가 구현

### 4. `MembershipService` 클래스
- 회원 목록 관리 (`List<Member>`)
- `addMember(Member)` / `printAllBenefits()` 메서드 포함

---

## 🚨 예외 처리 (선택 구현)

- 음수 결제 금액을 입력받으면 `InvalidAmountException` 발생

---

## 🎯 구현 목표 체크리스트

| 항목 | 포함 여부 |
|------|------------|
| 추상 클래스 설계 | ✅ |
| 메서드 오버라이딩 | ✅ |
| 인터페이스 활용 | ✅ |
| 다형성 기반 출력 처리 | ✅ |
| 정적 누적값 활용 (선택) | ✅ |
| 예외 처리 흐름 설계 (선택) | ✅ |
| 가독성 높은 출력 | ✅ |

---

## 📂 폴더 구조 예시

```
review-1-3-membership-grade/
├── README.md
└── src/
    └── com.review13/
        ├── Member.java
        ├── SilverMember.java
        ├── GoldMember.java
        ├── PlatinumMember.java
        ├── BenefitPrintable.java
        ├── MembershipService.java
        ├── InvalidAmountException.java (선택)
        └── Main.java
```

---

👉 기억 안 나는 개념은 얼마든지 수업자료 복습 가능!  
👉 온이에게 언제든 질문 OK!
