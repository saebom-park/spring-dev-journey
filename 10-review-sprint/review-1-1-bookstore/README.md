# REVIEW-1-1: 서점 주문 시스템 (bookstore)

> 🎯 **목표:** 지금까지 배운 Java 기본 문법을 종합적으로 복습하며  
> **클래스/상속/인터페이스/예외처리/static 개념을 통합**해보는 실습입니다.

---

## 💡 실습 시나리오

🛒 **온라인 서점 시스템 (콘솔 기반)**

- 도서를 관리하는 **Book**,  
- 회원을 나타내는 **Member**,  
- 그리고 회원의 구매 내역을 나타내는 **Order** 클래스를 직접 설계하고  
- 간단한 시뮬레이션 메서드를 작성해봅니다.

---

## 📋 구현 요구사항

### 1. `Book` 클래스
- `title`, `author`, `price` 필드
- 생성자 및 `toString()` 오버라이딩
- 정적 필드로 `totalBookCount`를 관리 (책 생성 시 1씩 증가)

### 2. `Member` 클래스
- `name`, `email` 필드
- 구매 메서드 `buyBook(Book book)` 구현
- 예외 상황: 책의 가격이 0원 이하일 경우 사용자 정의 예외 발생

### 3. `Order` 클래스
- `Member` 와 `Book` 간의 관계를 연결
- `Order`는 하나의 회원이 하나의 책을 주문한 것으로 표현
- 주문 정보를 출력하는 `printOrder()` 메서드 구현

### 4. 인터페이스 활용
- `Printable` 인터페이스 정의: `printInfo()` 추상 메서드 포함
- `Book`, `Order`가 이 인터페이스를 구현하도록 설정

---

## 🚨 예외 처리

- 가격이 0 이하인 책을 주문하려 하면  
  사용자 정의 예외 `InvalidPriceException` 발생시키기

---

## 🎯 구현 목표 체크리스트

| 항목 | 포함 여부 |
|------|------------|
| 클래스/객체 설계 | ✅ |
| 생성자 & 캡슐화 | ✅ |
| 정적 변수 사용 (`static`) | ✅ |
| 상속 또는 인터페이스 사용 | ✅ |
| 사용자 정의 예외 | ✅ |
| 오버라이딩 / 출력 형식 제어 | ✅ |
| 객체 간 관계 (has-a) | ✅ |

---

## 📂 폴더 구조 예시

```
review-1-java-core/
├── README.md
└── src/
    └── com.review1/
        ├── Book.java
        ├── Member.java
        ├── Order.java
        ├── InvalidPriceException.java
        ├── Printable.java
        └── Main.java
```

---

👉 구현 중 막히면 얼마든지 수업자료 복습 가능!  
👉 필요한 경우 온이에게 질문도 OK!  
