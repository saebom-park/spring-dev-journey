<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.review43.repository.ProductMapper">
    <resultMap id="ProductResultMap" type="com.review43.dto.ProductResponseDto">
        <result property="id" column="product_id"/>
        <result property="name" column="product_name"/>
        <result property="price" column="product_price"/>

        <association property="category" javaType="com.review43.dto.CategoryDto">
            <result property="id" column="category_id"/>
            <result property="name" column="category_name"/>
        </association>
    </resultMap>

    <select id="selectProductsWithCategory" resultMap="ProductResultMap">
        SELECT
            p.id AS product_id, p.product_name AS product_name, p.product_price AS product_price, p.category_id AS category_id, c.category_name AS category_name
        FROM products p
        JOIN categories c ON p.category_id = c.id
    </select>
</mapper>