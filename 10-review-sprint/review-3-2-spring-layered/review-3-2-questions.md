# [REVIEW-3-2] 질문노트: 인터페이스 분리 리팩토링

> 💬 봄이가 직접 궁금해서 물어본 것들 + 온이의 설명 요약
>
>
> (코드: MemberService.java 실습 기준)

---

### 💡 1. MemberService는 로직이 없는데 왜 그걸로 DI 받아?

| 질문 | 답변 요약 |
| --- | --- |
| MemberServiceImpl이 실제 로직을 갖고 있는데, 왜 인터페이스만 의존하지? | 우리는 구현이 아닌 "역할"에 의존하는 걸 원칙으로 해. MemberService는 행동의 약속을 정의하고, Spring은 그 약속을 만족하는 구현체(MemberServiceImpl)를 찾아서 주입해줘. 이렇게 하면 구조가 유연해지고, 테스트도 훨씬 편해져. |

---

### 💡 2. MemberService 구현체가 여러 개면 Spring은 뭘 주입해?

| 질문 | 답변 요약 |
| --- | --- |
| 같은 인터페이스를 구현한 클래스가 2개 이상이면 어떻게 돼? | 이 경우 Spring은 "하나를 고르지 못해" 오류(NoUniqueBeanDefinitionException)를 내. 해결 방법은 @Primary로 기본 구현체를 지정하거나, @Qualifier("이름")로 명시적으로 선택하거나, List로 전부 주입받아 선택적으로 사용하는 방식이 있어. |

---

### 💡 3. MemberServiceImpl에 @Service 안 붙이면 어떻게 돼?

| 질문 | 답변 요약 |
| --- | --- |
| MemberServiceImpl에 @Service 안 붙이고 실행했더니 에러가 났어 | @Service, @Repository 같은 어노테이션은 Spring이 Bean으로 등록하게 만드는 역할이야. 안 붙이면 Spring이 그 클래스를 컴포넌트로 인식하지 못해서 주입할 수 없어. 실행 시 NoSuchBeanDefinitionException이 터져. |

---

### 💡 4. MemberController에서 구현체(MemberServiceImpl) 대신 인터페이스로 주입하는 이유는?

| 질문 | 답변 요약 |
| --- | --- |
| 그냥 MemberServiceImpl로 주입하면 안돼? | 단기적으로는 가능하지만, 구조적으로 MemberService라는 역할에 의존해야 테스트, 유지보수, 확장성이 훨씬 좋아져. 추상화에 의존하는 게 객체지향의 핵심 원칙 중 하나야. |

---

### 🌱 정리 키워드

- MemberService는 "역할의 약속"
- Spring은 구현체를 찾아 주입 (DIP)
- 구현체 여러 개 → @Primary, @Qualifier, List
- Bean 등록 어노테이션(@Service/@Repository) 필수
- Controller는 구현체가 아닌 인터페이스에 의존

