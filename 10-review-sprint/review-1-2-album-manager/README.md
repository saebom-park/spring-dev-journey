# REVIEW-1-2: 앨범 관리 시스템 (album-manager)

> 🎯 **목표:** 객체지향의 핵심인  
> **인터페이스와 다형성 개념을 복습**하고,  
> 상황에 맞는 인터페이스 설계 능력을 키워보는 실습입니다.

---

## 💡 실습 시나리오

🎵 **앨범 관리 시스템 (콘솔 기반)**

- 음악 앨범을 관리하는 **Album**
- 다양한 형태의 앨범 (디지털, 실물) 을 표현하는 **인터페이스**
- 사용자(회원)가 앨범을 구매하는 **User**
- 구매 정보를 관리하는 **Purchase** 클래스를 설계하고  
- 다형성을 활용한 출력 기능을 구현합니다.

---

## 📋 구현 요구사항

### 1. `Album` 클래스
- `title`, `artist`, `price` 필드
- 생성자 및 `toString()` 오버라이딩
- `DigitalAlbum`, `PhysicalAlbum`이 상속받는 **부모 클래스 또는 상위 타입**으로 설계

### 2. `Sellable` 인터페이스
- `getDiscountPrice()` 메서드 정의
- `DigitalAlbum`, `PhysicalAlbum`이 각각 다르게 구현  
  - 디지털 앨범은 10% 할인  
  - 실물 앨범은 5% 할인

### 3. `DigitalAlbum` / `PhysicalAlbum` 클래스
- `Album`을 상속하고, `Sellable` 인터페이스 구현
- 각각의 앨범 타입에 맞는 할인 방식 구현

### 4. `User` 클래스
- `name`, `email` 필드
- `buyAlbum(Sellable album)` 메서드 구현  
  → 앨범을 구매하고 구매 목록에 추가

### 5. `Purchase` 클래스
- 구매자 정보와 구매 앨범 리스트를 저장
- `printPurchaseInfo()` 메서드로 전체 구매 정보 출력

---

## 🚨 예외 처리 (선택 구현)

- 가격이 0 이하일 경우 `InvalidPriceException` 발생시키기  
  (REVIEW-1-1에서 작성한 예외 클래스 재활용 가능)

---

## 🎯 구현 목표 체크리스트

| 항목 | 포함 여부 |
|------|------------|
| 클래스/객체 설계 | ⬜ |
| 상속/인터페이스 활용 | ⬜ |
| 메서드 오버라이딩 | ⬜ |
| 다형성 기반 호출 | ⬜ |
| 할인 정책 분기 처리 | ⬜ |
| 사용자 정의 예외 (선택) | ⬜ |
| 출력 책임 분리 (`printInfo`) | ⬜ |

---

## 📂 폴더 구조 예시

```
review-1-2-album-manager/
├── README.md
└── src/
    └── com.review12/
        ├── Album.java
        ├── DigitalAlbum.java
        ├── PhysicalAlbum.java
        ├── Sellable.java
        ├── User.java
        ├── Purchase.java
        ├── InvalidPriceException.java (선택)
        └── Main.java
```

---

👉 구현 중 막히면 수업자료 복습 가능!  
👉 필요한 경우 온이에게 질문도 OK!
