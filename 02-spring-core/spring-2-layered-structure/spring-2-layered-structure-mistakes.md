# [SPRING-2단계] 실수노트

> 💻 실습 코드: springlab5
> 

---

### 😅 실수 1 — `OrderService` Bean 주입 실패

```
에러 메시지: No qualifying bean of type 'com.springlab5.OrderService'
```

✅ **원인:** `@Component` 누락으로 인해 스프링이 Bean으로 등록하지 못함

✅ **해결:** 클래스 상단에 `@Component` 어노테이션 추가하여 정상 등록 완료

📌 **설명:**

- 스프링에서 Bean으로 관리되려면 반드시 `@Component`, `@Service`, `@Repository` 등의 어노테이션이 필요
- 누락 시 의존성 주입 시점에 Bean을 찾지 못해 NoSuchBeanDefinitionException 발생

---

### 😅 실수 2 — 콘솔에 `123L` 입력 시 런타임 에러 발생

```
에러 메시지: InputMismatchException 또는 NumberFormatException
```

✅ **원인:** 콘솔 입력값은 문자열로 처리되므로 `L` 포함 숫자는 파싱 불가

✅ **해결:** 사용자에게는 `123` 형태로 입력하도록 안내하고, 내부에서 `Long`으로 파싱하도록 유지

📌 **설명:**

- `Scanner.nextLong()`은 숫자만 허용함. `123L`처럼 `L`이 포함되면 숫자 아님으로 간주되어 예외 발생
- 코드 내부에서는 `123L`처럼 L 접미사를 써도 되지만, 사용자 입력은 숫자 그대로 받아야 함

---

### 😅 실수 3 — `non-zero exit value 1` 실행 실패

```
에러 메시지: Execution failed for task ':com.springlab5.App.main()'
```

✅ **원인:** 위와 같은 예외가 발생하여 JVM이 비정상 종료됨

✅ **해결:** `Caused by:` 로그를 먼저 확인하여 실제 원인을 추적하고 수정함

📌 **설명:**

- `non-zero exit`은 단독 원인이 아니라 “무언가 실패했다”는 후속 메시지
- 예외 메시지에서 가장 중요한 건 `Caused by:` 이후의 라인 → 거기서 진짜 원인이 나온다

---

### 📌 실수 요약 포인트

- 어노테이션 누락은 NoSuchBeanDefinitionException의 주요 원인
- 콘솔 입력은 `Long` 타입이라도 `123`처럼 숫자만 입력해야 함
- `non-zero exit` 에러는 에러가 있다는 결과일 뿐, 위쪽 로그가 핵심
- 에러 로그는 항상 `Caused by:`를 기준으로 추적할 것