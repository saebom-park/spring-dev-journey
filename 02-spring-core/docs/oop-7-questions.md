# [OOP-7단계] 질문노트: 인터페이스 (SmartWatch.java)

> 💬 봄이가 직접 궁금해서 물어본 것들 + 온이의 설명 요약
> 
> 
> (코드: SmartWatch.java 실습 기준)
> 

---

### 💡 1. 질문 주제 요약

| 질문 | 답변 요약 |
| --- | --- |
| 인터페이스도 클래스처럼 한 파일에 하나만 정의해야 해? | ✅ 실무에서는 인터페이스도 **한 파일에 하나씩** 정의하는 게 기본 컨벤션이야. 유지보수와 협업 편의성을 위해 파일명과 타입명이 1:1로 매칭되는 게 좋음. |
| 인터페이스 메서드에는 왜 public을 안 붙여도 돼? | 자바에서 인터페이스 안의 메서드는 자동으로 **public abstract**가 붙어 있어. 생략 가능하지만, 구현 클래스에서는 반드시 public으로 오버라이딩해야 함. |
| int형으로 입력받을 때 문자 입력하면 어떻게 돼? | `Scanner.nextInt()`는 문자열 입력 시 **InputMismatchException**, `Integer.parseInt()`는 **NumberFormatException**이 발생해. |
| Integer.parseInt()는 무슨 뜻이야? | `"숫자처럼 생긴 문자열"`을 **정수(int)** 로 변환하는 메서드야. `Integer.parseInt("123")` → `123` |
| 정해진 값이 아닌 걸 입력했을 땐 어떤 예외를 써야 해? | 자바에서 자동 예외는 발생하지 않기 때문에, 조건을 직접 검사하고 `IllegalArgumentException`을 **직접 던지는 방식**을 사용해야 해. |

---

### 🌱 정리 키워드

- 인터페이스 구현은 `implements` 키워드 사용
- 인터페이스 메서드는 자동으로 `public abstract`
- 문자열 → 정수 변환은 `Integer.parseInt()`
- 유효하지 않은 입력 → `IllegalArgumentException` 사용