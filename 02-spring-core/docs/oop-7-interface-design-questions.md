# [OOP-7단계] 질문노트: 인터페이스 (SmartWatch.java)

> 💬 봄이가 직접 궁금해서 물어본 것들 + 온이의 설명 요약  
> (코드: SmartWatch.java 실습 기준)

---

### 💡 1. 인터페이스도 클래스처럼 한 파일에 하나만 정의해야 해?

| 질문 | 답변 요약 |
|------|-----------|
| 인터페이스도 클래스처럼 한 파일에 하나만 써야 해? | ✅ 실무에서는 인터페이스도 **한 파일에 하나씩** 정의하는 게 기본 컨벤션이야. 유지보수와 협업 편의성을 위해 파일명과 타입명이 1:1로 매칭되는 게 좋아. |

---

### 💡 2. 인터페이스 메서드에는 왜 `public` 안 붙여도 돼?

| 질문 | 답변 요약 |
|------|-----------|
| 인터페이스 메서드는 public 안 붙여도 되는 이유가 뭐야? | 자바에서 인터페이스 안의 메서드는 자동으로 **`public abstract`**가 붙어 있어. 생략 가능하지만, 구현 클래스에서는 반드시 public으로 오버라이딩해야 해. |

---

### 💡 3. `Scanner.nextInt()`에서 문자 입력하면 무슨 일이 생겨?

| 질문 | 답변 요약 |
|------|-----------|
| 숫자 대신 문자를 입력하면? | `Scanner.nextInt()`는 문자열 입력 시 **`InputMismatchException`**, `Integer.parseInt()`는 **`NumberFormatException`**이 발생해. 방식마다 예외가 달라! |

---

### 💡 4. `Integer.parseInt()`는 무슨 뜻이야?

| 질문 | 답변 요약 |
|------|-----------|
| 문자열을 숫자로 바꾸는 거야? | 맞아! `"숫자처럼 생긴 문자열"`을 **정수(int)** 로 변환하는 메서드야. 예: `Integer.parseInt("123")` → `123` |

---

### 💡 5. 유효하지 않은 입력에는 어떤 예외를 던져야 해?

| 질문 | 답변 요약 |
|------|-----------|
| 조건에 안 맞는 입력엔 어떤 예외를 써야 해? | 자바에서 자동 예외는 안 생겨. 그래서 **조건을 직접 검사해서** `IllegalArgumentException` 같은 표준 예외를 **직접 던져야 해.** |

---

### 🌱 정리 키워드

- 인터페이스 구현 → `implements` 사용
- 인터페이스 메서드는 자동으로 `public abstract`
- 문자열 → 숫자 변환 → `Integer.parseInt()`
- 숫자 입력 예외 → `InputMismatchException`, `NumberFormatException`
- 직접 검사 후 예외 던질 땐 → `IllegalArgumentException`
