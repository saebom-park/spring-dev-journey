# [OOP-2단계] 캡슐화와 접근 제어자

> “클래스 속 정보를 숨기고 관리하는 방법은?”
> 
> 
> 👉 바로 **캡슐화 + 접근 제어자 (private, public)**!
> 

---

### 💡 핵심 개념 요약

| 개념 | 설명 |
| --- | --- |
| 캡슐화 | 데이터(필드)를 숨기고, 메서드로만 접근하도록 만드는 원리 |
| 접근 제어자 | 클래스, 변수, 메서드의 공개 수준을 조절하는 키워드 |
| `private` | 클래스 내부에서만 접근 가능 (외부 차단) |
| `public` | 모든 클래스에서 접근 가능 |
| `getter/setter` | `private` 필드에 접근하는 안전한 창구 역할의 메서드 |

---

### 🧾 예시 코드

```java
public class Person {
    private String name;  // 외부에서 직접 접근 불가
    private int age;

    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }

    // getter
    public String getName() {
        return name;
    }

    // setter
    public void setAge(int age) {
        if (age >= 0) {
            this.age = age;
        }
    }

    public void introduce() {
        System.out.println("안녕하세요! 저는 " + name + ", 나이는 " + age + "살이에요.");
    }

    public static void main(String[] args) {
        Person p = new Person("봄이", 25);
        p.setAge(26);  // setter로 값 변경
        System.out.println("이름: " + p.getName()); // getter로 읽기
        p.introduce();
    }
}
```

---

### 📌 포인트 요약

- **직접 접근 차단 (`private`) + 간접 접근 허용 (`getter/setter`)** → 이것이 캡슐화
- 잘못된 값 방지 (`setAge` 조건처럼)
- 실무에서도 DTO, VO 객체는 대부분 이렇게 캡슐화함

---

### 🧪 실습 미션

> 🎯 목표: Student 클래스를 만들어 캡슐화 실습하기!
> 
1. `Student` 클래스 만들기
    - `name`과 `score`는 `private`으로 선언
    - 생성자에서 초기화
2. `getter` 메서드 2개 만들기
    - `getName()`, `getScore()`
3. `setter` 메서드 1개 만들기
    - `setScore(int score)` → 0~100점 사이만 허용
4. `main()`에서 객체 생성 & setter로 값 변경 후 출력