# [OOP-4단계] 오버라이딩 개념

> “부모가 물려준 기능을,
> 
> 
> 내 상황에 맞게 바꾸고 싶을 땐?”
> 

> 👉 바로 @Override로 부모의 메서드를 자식이 재정의하는 것!
> 

---

### 💡 핵심 개념 요약

| 개념 | 설명 |
| --- | --- |
| 오버라이딩 (Overriding) | 부모 클래스의 메서드를 자식 클래스에서 **재정의**하는 것 |
| `@Override` | 오버라이딩을 명시해주는 어노테이션 (선택이지만 **필수처럼 쓰임**) |
| 조건 | 메서드 이름, 매개변수, 리턴타입이 **부모와 완전히 동일해야 함** |
| 실행 흐름 | 오버라이딩된 메서드는 **자식 클래스의 것이 우선 실행**됨 |
| super.메서드() | 필요 시 부모의 메서드도 **자식에서 호출 가능** |

---

### 🧾 예시 코드

```java
class Animal {
    public void speak() {
        System.out.println("동물이 소리를 냅니다.");
    }
}

class Cat extends Animal {
    @Override
    public void speak() {
        System.out.println("야옹!");
    }
}

public class OverrideExample {
    public static void main(String[] args) {
        Animal a1 = new Animal();
        Animal a2 = new Cat();

        a1.speak(); // 동물이 소리를 냅니다.
        a2.speak(); // 야옹! ← 오버라이딩된 메서드가 실행됨
    }
}
```

---

### 📌 포인트 요약

- 오버라이딩은 상속받은 메서드를 **내 상황에 맞게 다시 구현하는 것**
- `@Override`를 꼭 붙이면 실수 방지 + 가독성 향상
- 실행 시에는 **자식 클래스의 메서드가 우선 실행됨**
- 부모 메서드도 필요하면 `super.메서드()`로 호출 가능
- 실무에서 오버라이딩은 **동작 다형성 구현의 핵심 요소**

---

### 🧪 실습 미션

> 🎯 목표: 오버라이딩 개념을 확실히 체득!
> 
1. `MessageSender` 클래스: `send()` 메서드에서 `"Sending a message..."` 출력
2. `EmailSender` 클래스: `send()`를 오버라이딩해서 `"Sending an email."` 출력
3. `SMSSender` 클래스: `send()` 오버라이딩 → `"Sending a text message."` 출력
4. `SenderExample` 클래스에서 `MessageSender` 타입 배열로
    
    `EmailSender`, `SMSSender` 객체를 순차 실행해보기