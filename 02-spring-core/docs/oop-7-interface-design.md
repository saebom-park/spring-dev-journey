# [OOP-7단계] 인터페이스 설계

> “추상 클래스는 기능 틀을 제공하지만,
> 
> 
> 여러 기능을 조합하고 싶다면?”
> 

👉 바로 **인터페이스(interface)** 를 써서

**다중 구현 / 강제 규칙 / 느슨한 연결**을 설계하는 거야!

---

### 💡 핵심 개념 요약

| 개념 | 설명 |
| --- | --- |
| `interface` | 구현할 메서드의 "규칙만 선언"하는 설계 도구 |
| `implements` | 클래스가 인터페이스를 **구현(구체화)** 할 때 사용 |
| 다중 구현 | 자바는 **클래스는 하나만 상속**, 인터페이스는 **여러 개 구현 가능** |
| 목적 | **강제성 있는 역할 분리 + 확장 가능성 높은 구조 설계** |

---

### 🧾 예시 코드

```java
interface Charger {
    void charge();
}

interface Browser {
    void browse();
}

class SmartDevice implements Charger, Browser {
    @Override
    public void charge() {
        System.out.println("🔋 충전 중...");
    }

    @Override
    public void browse() {
        System.out.println("🌐 웹 브라우징 중...");
    }

    public static void main(String[] args) {
        SmartDevice device = new SmartDevice();
        device.charge();
        device.browse();
    }
}
```

---

### 📌 포인트 요약

- 인터페이스는 **"무조건 구현해야 하는 메서드 목록"을 정의**
- `implements` 키워드로 구현
- 다중 인터페이스 구현 가능 → **유연한 설계 구조**
- 실무에서는 **서비스 역할 분리, 전략 패턴 등에서 자주 등장**

---

### 🧪 실습 미션

> 🎯 목표: 인터페이스 다중 구현 연습 + 기능 분리 이해하기
> 
1. `Camera` 인터페이스: `takePhoto()` 선언
2. `MusicPlayer` 인터페이스: `playMusic()` 선언
3. `SmartWatch` 클래스에서 **두 인터페이스 모두 구현**
    - 각각의 메서드를 `System.out.println()`으로 출력
4. `SmartWatchExample`에서 객체 생성 후
    - `takePhoto()` & `playMusic()` 호출