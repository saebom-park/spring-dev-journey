# [OOP-2단계] 실수노트

> 💻 실습 코드: Student.java
> 

---

### 😅 실수 1 — 같은 이름 클래스 중복 오류 (Person)

```java
// 01-core-java에 Person.java가 이미 존재한 상태
public class Person {
    ...
}
```

✅ 정답:

```java
// 패키지를 명확히 선언하고, 다른 위치로 분리
package oop;

public class Person {
    ...
}
```

📌 **설명**:

- IntelliJ는 모든 `src` 폴더를 **하나의 클래스 경로로 간주**
- 동일한 이름의 클래스가 패키지 없이 겹치면 `duplicate class` 오류 발생함

---

### 😅 실수 2 — 패키지 선언과 실제 경로가 다를 때 오류 발생

```java
package oop; // 선언은 했지만...

// 실제 파일 경로: src/Student.java
```

✅ 정답:

```java
// 실제 경로: src/oop/Student.java로 이동
```

📌 **설명**:

- 자바는 `package` 선언과 실제 폴더 경로가 **정확히 일치**해야 함
- 안 맞으면 컴파일러가 "패키지 맞지 않음" 오류를 냄

---

### 📌 요약 포인트

- 동일 클래스명 충돌 시 `duplicate class` 발생
- `package` 선언은 **경로와 꼭 일치**해야 함
- IntelliJ는 프로젝트 전체를 하나의 빌드 경로로 본다