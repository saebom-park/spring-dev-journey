# [10단계] 상속 (Inheritance)

> 자식 클래스가 부모 클래스를 extends로 상속받아,
> 
> 
> 변수/메서드를 물려받고 `@Override`로 오버라이딩할 수 있어요.
> 
> 코드 재사용과 유연한 설계의 핵심, 바로 객체지향의 "상속"! 🧬
> 

---

### 💡 핵심 개념 요약

| 개념 | 설명 |
| --- | --- |
| `extends` | 부모 클래스를 확장해서 자식 클래스 정의 |
| `super()` | 자식 클래스에서 부모 클래스의 생성자를 호출 |
| `@Override` | 부모의 메서드를 자식이 덮어쓸 때 붙이는 어노테이션 |
| **is-a 관계** | 자식 is-a 부모 → Dog is-a Animal |
| 상속의 목적 | 코드 재사용, 계층적 구조 설계, 공통 기능 추출 |

---

### 🧾 예시 코드 ① 기본 상속

```java
// 부모 클래스
class Animal {
    String name;

    void speak() {
        System.out.println("동물이 소리를 낸다");
    }
}

// 자식 클래스
class Dog extends Animal {
    @Override
    void speak() {
        System.out.println("멍멍!");
    }
}

public class Main {
    public static void main(String[] args) {
        Dog d = new Dog();
        d.name = "초코";
        d.speak(); // 멍멍!
        System.out.println("이름: " + d.name);
    }
}
```

---

### 📌 포인트 요약 ①

- `extends`: 상속의 핵심 키워드
- 자식 클래스는 부모의 변수/메서드 자동 상속
- `@Override`는 재정의 확인용 (실수 방지)
- 상속은 **is-a 관계**가 성립할 때만 설계 (예: 강아지는 동물이다)

---

### 🧾 예시 코드 ② `super()`로 생성자 상속

```java
class Animal {
    String name;
    int age;

    Animal(String name, int age) {
        this.name = name;
        this.age = age;
    }

    void introduce() {
        System.out.println("안녕하세요! 제 이름은 " + name + "이고, 나이는 " + age + "살입니다.");
    }
}

class Dog extends Animal {
    Dog(String name, int age) {
        super(name, age); // 부모 생성자 호출!
    }

    void speak() {
        System.out.println("멍망몽멍!");
    }
}

public class Main {
    public static void main(String[] args) {
        Dog aboo = new Dog("아부", 7);
        aboo.introduce(); // 안녕하세요! 제 이름은 아부이고, 나이는 7살입니다.
        aboo.speak();     // 멍망몽멍!
    }
}
```

---

### 📌 포인트 요약 ②

- 부모 클래스에 **생성자**가 있으면 자식 클래스는 `super(...)`로 반드시 호출해야 함
- `super(...)`는 **생성자 첫 줄**에 위치해야 함
- `this.필드 = 값`과 같은 역할을 부모에게 위임 → 코드 재사용
- 매개변수의 개수·순서·타입은 부모 생성자와 정확히 일치해야 함

---

### 🧪 실습 미션

> ✅ 아래 조건을 만족하는 상속 구조를 직접 작성해보자!
> 
1. `Animal` 클래스 → `String name`, `int age` 필드 포함
2. 생성자 `Animal(String name, int age)` 작성
3. `Dog` 클래스 → `extends Animal` + `super(name, age)` 호출
4. `Main` 클래스 → `new Dog("아부", 7)` 객체 생성
5. `introduce()`와 `speak()` 호출

🧾 예상 출력:

```
안녕하세요! 제 이름은 아부이고, 나이는 7살입니다.
멍망몽멍!
```