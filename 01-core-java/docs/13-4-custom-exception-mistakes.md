# [13-4단계] 실수노트

> 💻 실습 코드: SignUpService.java
> 

---

### 😅 실수 1 — `throws`를 클래스 선언부에 작성함

```java

public class SignUpService throws CustomAgeException {
    ...
}
```

✅ 정답:

```java
public void signUp(String name, int age) throws CustomAgeException {
    ...
}
```

📌 **설명**:

- `throws`는 클래스가 아닌 **메서드 선언부에만 사용할 수 있어!**
- 예외를 던지는 주체는 메서드이기 때문에
    
    `throw`와 `throws`는 반드시 **메서드 안에서만** 다뤄야 해!
    

---

### 📌 요약 포인트

- `throws`는 클래스에 붙이는 키워드가 아님
- 예외를 던지는 주체는 항상 **메서드**
- 메서드 선언부에서만 `throws` 사용 가능
- 헷갈리더라도 예외 선언은 항상 **메서드 위주로 생각하기**