# [14-1단계] static 변수 vs 인스턴스 변수

> ❓ “모든 객체가 공유하는 변수는 어떻게 만들까?”
> 
> 
> ✅ **“바로 `static` 키워드를 붙이면 돼!”**
> 

---

## 💡 핵심 개념 요약

| 구분 | static 변수 | 인스턴스 변수 |
| --- | --- | --- |
| 정의 방식 | `static` 키워드를 붙여 선언 | 그냥 클래스 내부에서 선언 |
| 메모리 위치 | 클래스가 로딩될 때 메모리 1번 할당 | 객체가 생성될 때마다 새로 생성 |
| 소속 | 클래스에 소속 | 객체(인스턴스)에 소속 |
| 사용 방법 | `클래스명.변수명` or `인스턴스.변수명` | `인스턴스.변수명`만 가능 |
| 대표 예시 | 공용 설정값, 카운터, 로깅 변수 등 | 개인 점수, 이름, 상태 등 |

---

## 🧾 예시 코드

```java
public class Counter {
    static int globalCount = 0;       // 모든 객체가 공유
    int localCount = 0;              // 각 객체마다 따로 존재

    public void increase() {
        globalCount++;
        localCount++;
    }

    public void print() {
        System.out.println("globalCount: " + globalCount);
        System.out.println("localCount: " + localCount);
    }
}
```

```java
public class CounterMain {
    public static void main(String[] args) {
        Counter a = new Counter();
        Counter b = new Counter();

        a.increase();  // global +1, local +1 (a)
        b.increase();  // global +1, local +1 (b)

        a.print();     // global: 2, local: 1
        b.print();     // global: 2, local: 1
    }
}
```

---

## 📌 포인트 요약

- `static` 변수는 객체와 관계없이 **클래스에 직접 속한 변수**
- 모든 인스턴스가 **같은 값을 공유**함
- 객체마다 달라야 하는 값은 static 없이 **인스턴스 변수로 선언**
- `클래스명.변수명`으로 접근하는 게 가장 명확한 스타일

---

## 🧪 실습 미션

> 🎯 목표: static과 인스턴스 변수의 차이를 직접 확인해보기
> 
1. `Person` 클래스를 만들자
    - `name`: 인스턴스 변수
    - `population`: static 변수 (전 인구 수 추적)
2. 생성자에서 `population++` 하고, `name`은 생성자 파라미터로 전달
3. 3명의 Person을 만들고 각각 이름과 population을 출력해보자

```java
Person p1 = new Person("봄이");
Person p2 = new Person("온이");
Person p3 = new Person("솔이");
```