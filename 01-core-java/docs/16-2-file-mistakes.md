# [16-2단계] 실수노트

> 💻 실습 코드: FilePractice.java
> 

---

### 😅 실수 1 — `SampleFile.txt` 경로 누락

```java
File file = new File("SampleFile.txt"); // ❌ 찾을 수 없음!
```

✅ 정답:

```java
File file = new File("01-core-java/src/SampleFile.txt");
```

📌 **설명**:

- 실행 기준 경로는 프로젝트 루트 (`C:\\dev\\spring-dev-journey`)
- 파일이 `src/`에 있다면 그 경로까지 포함해줘야 정상적으로 찾을 수 있음

---

### 😅 실수 2 — 확장자 누락으로 인한 파일 검색 실패

```
입력한 경로: C:\\dev\\spring-dev-journey\\01-core-java\\src\\SampleFile
결과: 파일이 존재하지 않습니다.
```

✅ 정답:

```
입력한 경로: C:\\dev\\spring-dev-journey\\01-core-java\\src\\SampleFile.txt
결과: 파일이 존재합니다.
```

📌 **설명**:

- 자바의 `File`은 **문자열 그대로 해석**하기 때문에,
실제 파일 이름과 정확히 일치해야 찾을 수 있음
- `.txt`가 빠지면 다른 이름의 파일을 찾게 됨

---

### 📌 요약 포인트

- `File` 경로는 문자열 기반 → 철자 하나라도 다르면 못 찾음
- 실행 기준 디렉토리는 항상 `System.getProperty("user.dir")`로 확인
- `.txt` 등 확장자 누락 여부 항상 체크!