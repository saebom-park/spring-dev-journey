# [14-2단계] static 메서드

> ❓ “객체 없이도 호출 가능한 메서드는?”
> 
> 
> ✅ **“바로 `static` 메서드!”**
> 

---

## 💡 핵심 개념 요약

| 구분 | static 메서드 | 인스턴스 메서드 |
| --- | --- | --- |
| 소속 | 클래스 자체 | 생성된 객체(instance) |
| 호출 방식 | 클래스명.메서드명() | 객체명.메서드명() |
| 객체 필요 여부 | ❌ 필요 없음 | ✅ 필요함 |
| 사용 목적 | 공용 기능, 유틸 함수, 팩토리 메서드 | 상태 조작, 데이터 의존 로직 |
| 제약 사항 | 인스턴스 필드/메서드 접근 ❌ | 자유롭게 사용 가능 |

---

## 🧾 예시 코드

```java
public class MathUtil {
    public static int doubleValue(int n) {
        return n * 2;
    }

    public int tripleValue(int n) {
        return n * 3;
    }
}
```

```java
public class MathMain {
    public static void main(String[] args) {
        // static 메서드는 객체 없이 호출 가능
        int a = MathUtil.doubleValue(5);
        System.out.println("2배: " + a);

        // 인스턴스 메서드는 객체 생성 후 호출
        MathUtil util = new MathUtil();
        int b = util.tripleValue(5);
        System.out.println("3배: " + b);
    }
}
```

---

## 📌 포인트 요약

- `static` 메서드는 **객체 없이도 호출 가능**한 클래스 기반 메서드
- 주로 **유틸리티 기능**에 많이 사용됨 (`Math.random()`, `Integer.parseInt(...)`)
- 객체 상태와 무관한 로직은 static으로 만드는 게 더 깔끔하고 효율적
- **인스턴스 필드나 메서드에는 접근할 수 없음!**

---

## 🧪 실습 미션

> 🎯 목표: static 메서드의 특성을 체험해보자!
> 
1. `Converter` 클래스를 만들자
    - static 메서드 `cmToMm(int cm)` → cm를 mm로 변환
    - static 메서드 `kgToG(int kg)` → kg를 g으로 변환
2. `ConverterMain`에서 static 메서드를 객체 없이 호출하고 출력하자

```java
int mm = Converter.cmToMm(123); // → 1230 출력
int g = Converter.kgToG(4);     // → 4000 출력
```