# [DBA-4-1단계] 실수노트: 단방향 연관관계 매핑

💻 실습 코드: application.yml, Order.java

---

### 😅 실수 1 — Hibernate SQL 포맷 설정 오타: `format_sql` → `format-sql`

```
spring:
  jpa:
    properties:
      hibernate:
        format-sql: true  # ❌ 오타! 설정 적용 안 됨
```

✅ 정답:

```
spring:
  jpa:
    properties:
      hibernate:
        format_sql: true  # ✅ 언더스코어로 정확히 작성해야 함
```

**📌 설명:**

- Hibernate 설정 키는 공식 속성명 그대로 작성해야 함
- `format_sql`은 하이픈(-)이 아닌 언더스코어(_)로 작성해야 적용됨
- 오타가 나도 경고 없이 무시되기 때문에 특히 주의 필요!

---

### 😅 실수 2 — `@Table(name = "name")`처럼 테이블명을 구체적이지 않게 지정함

```
@Entity
@Table(name = "name")  // ❌ 모호한 테이블명
public class Order { ... }
```

✅ 정답:

```
@Entity
@Table(name = "orders")  // ✅ 실무에서는 복수형, 명확한 테이블명 사용
public class Order { ... }
```

**📌 설명:**

- `name`, `type` 같은 단어는 SQL에서 예약어나 컬럼명으로 흔하게 사용돼 혼란을 줄 수 있어
- 테이블명은 보통 `orders`, `products`, `members`처럼 **복수형 + 명확한 명사**로 구성하는 게 유지보수에 유리함
- 실무에서는 혼동 방지를 위해 구체적인 네이밍 컨벤션을 정해서 일관되게 사용해

---

### 📌 요약 포인트

- Hibernate 설정 키는 하이픈(-)이 아닌 언더스코어(_)로 작성해야 적용됨 (`format_sql`)
- 테이블명은 `name`, `type` 같은 모호한 단어가 아닌 구체적인 명사(복수형)로 작성해야 함