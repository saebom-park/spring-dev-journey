<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springlab14.practice.OrderMapper">

    <resultMap id="orderResultMap" type="Order">
        <id property="id" column="order_id"/>
        <result property="item_name" column="item_name"/>
        <result property="price" column="price"/>
    </resultMap>

    <select id="findAll" resultMap="orderResultMap">
        SELECT order_id, item_name, price FROM orders
    </select>

    <select id="findById" resultMap="orderResultMap" parameterType="int">
        SELECT order_id, item_name, price FROM orders WHERE order_id = #{id}
    </select>

    <insert id="insert" parameterType="Order">
        INSERT INTO orders (item_name, price)
        VALUES (#{item_name}, #{price})
    </insert>

    <select id="findByCondition" resultMap="orderResultMap">
        SELECT order_id, item_name, price FROM orders
        <where>
            <if test="item_name != null">
                item_name = #{item_name}
            </if>
            <if test="price != null">
                AND price &gt;= #{price}
            </if>
        </where>
    </select>

</mapper>